# Service Implementations

This directory contains concrete handwritten service implementations.

## Contents

- `FilmService.js`: in-memory service used by both generated server stubs. It supports listing films, retrieving a film by ID, creating a film, and deleting a film.

This service represents the business logic of the example API. Both generated servers call it indirectly through their corresponding adapter.
