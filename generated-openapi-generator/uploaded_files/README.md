# Uploaded Files

This directory is created by the generated OpenAPI Generator server as the default location for uploaded files.

The current DSP Film API experiment does not use file upload endpoints, so this directory is not central to the comparison. It remains part of the generated project structure.
